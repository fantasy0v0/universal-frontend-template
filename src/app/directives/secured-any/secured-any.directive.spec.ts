import { SecuredAnyDirective } from './secured-any.directive';

describe('SecuredAnyDirective', () => {
  it('should create an instance', () => {
    const directive = new SecuredAnyDirective();
    expect(directive).toBeTruthy();
  });
});
